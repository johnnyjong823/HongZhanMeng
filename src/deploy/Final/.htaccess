DirectoryIndex index.php

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /Final/
    
    # 將所有請求導向 public 目錄
    RewriteCond %{REQUEST_URI} !^/Final/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

Options -Indexes
